//FizzBuzz Prompt:
//Create a function that, given an integer "n", prints the numbers from 1 to n with the following conditions:
//1. If the number is divisible by 3, print "Fizz" instead of the number.
//2. If the number is divisible by 5, print "Buzz" instead of the number.
//3. If the number is divisible by 3 and 5, print "FizzBuzz"
//4. If none of the above conditions are met, print the number.

function FizzBuzz(num) {
  for (let v = 1; v <= num; v += 1) {
    if (v % 3 === 0 && v % 5 === 0) {
      console.log("FizzBuzz");
    } else if (v % 5 === 0) {
      console.log("Buzz");
    } else if (v % 3 === 0) {
      console.log("Fizz");
    } else {
      console.log(v);
    }
  }
}

FizzBuzz(30);
