/* 
Create a function that returns the area of a triangle, given the base and height as parameters.
Area = (base * height) / 2
*/

function triangleArea(base, height){
  let area = (base * height) / 2;
  console.log(area);
  return area;
}

triangleArea(3, 5);
