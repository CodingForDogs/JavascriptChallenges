// Create a function that accepts an array and returns the sum of all numbers in the array

let sum = 0;
const numbers = [3, 2, 5, 10];

numbers.forEach(sumOfElements);

function sumOfElements(item) {
  sum += item;
}

console.log(sum)
